/**
 * Build-time constants, substituted by Vite (see `define` in vite.config.ts).
 * They exist so a bug report can say which build it came from.
 */
declare const __APP_VERSION__: string;
declare const __BUILD_TIME__: string;
